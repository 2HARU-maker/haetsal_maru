# 햇살마루 작업 원칙

이 지침은 햇살마루 저장소에서 수행하는 작업 전반에 적용한다. 운영 데이터 보호, 기존 동작 보존, 명시적으로 승인된 범위 준수를 최우선으로 하며 저장소 전체를 특정 테스트 환경이나 Emulator 전용 구조로 가정하지 않는다.

## 적용 범위와 승인

- 기본은 분석 우선이다. 사용자가 증상이나 문제만 설명한 경우 원인과 영향 범위를 조사하되 임의로 코드를 수정하지 않는다.
- 사용자가 `수정해`, `적용해`, `그대로 진행해`, `승인` 등으로 명확히 허락한 범위만 수정한다.
- 승인 범위를 넘어선 문제를 발견하면 수정하지 않고 `발견했지만 이번 수정 범위에는 포함하지 않겠습니다.`라고 별도로 보고한다.
- 완료 여부가 명확하지 않은 과거 문제는 완료로 추정하지 않고 미해결 상태로 취급한다.
- 기존 파일과 사용자 변경사항을 보존한다. 현재 지시와 무관한 파일은 수정하지 않는다.
- 작업 전에 필요한 파일, 디렉터리, 브랜치, 스크립트, 명령이 실제 현재 작업환경에 존재하는지 확인한다.
- `README_FIRST.md`, `CODEX_FIRST_RUN_INSTRUCTIONS.txt`, `firestore.rules`, `index_emulator_only.html`, `package.json`, lockfile을 포함한 특정 파일이나 구조는 존재가 확인된 경우에만 참조한다.

## 기술 구조와 변경 원칙

- 현재 햇살마루 앱의 순수 HTML, CSS, JavaScript 구조를 유지한다.
- iframe, 문자열 끼워넣기 방식의 우회 구조, 외부 프레임워크를 임의로 도입하지 않는다.
- 새로운 라이브러리, 패키지, 빌드 도구, 프레임워크, npm 의존성은 사용자 승인 없이 추가하지 않는다.
- 기존 기능과 디자인을 최대한 보존하고 문제 해결에 필요한 최소 수정 범위를 우선한다.
- 관계없는 리팩터링, 포맷 변경, 이름 변경, 코드 정리, 파일 이동은 하지 않는다.
- 실패를 숨기거나 검증을 통과시키기 위한 임시 우회 수정을 하지 않는다.

## 문제 분석 원칙

수정 전 가능한 범위에서 다음을 확인한다.

1. 실제 재현 경로
2. 관련 함수, 이벤트, 데이터 흐름
3. 문법 및 런타임 오류
4. 누락, 중복, 충돌 로직
5. CSS 우선순위와 화면 구조
6. Firestore, 로컬 상태, 캐시, 오프라인 큐 등 상태 흐름
7. 수정이 기존 기능과 데이터에 미칠 영향

- 검증 전에는 원인을 확정적으로 단정하지 않는다.
- 추측보다 코드, 로그, 실제 재현, 테스트 결과를 우선한다.
- 분석이나 재현만 요청받은 경우에는 결과를 보고하고, 수정 승인을 받기 전까지 코드를 변경하지 않는다.

## 기능 우선순위

- 현재 학교 카페 운영에 필요한 오류와 기능 수정이 항상 1순위다.
- 향후 협동조합 운영 가능성을 고려할 수는 있지만 미래 확장을 이유로 현재 필요한 수정을 미루지 않는다.
- 금방 끝낼 수 있는 간단한 수정은 불필요하게 별도 후순위로 미루지 않는다.
- 이전 작업에서 제기됐더라도 수정과 검증 완료가 명확하지 않은 항목은 미해결 상태로 취급한다.

## 운영 Firebase 보호

- 운영 Firebase 프로젝트 `haetsal-maru-24b95`에는 테스트 목적으로 읽기, 쓰기, 삭제, 배포를 포함한 접근을 하지 않는다.
- `firebase deploy`, Firebase Hosting 배포, Firestore Rules 배포 등 운영 상태를 변경하는 명령은 사용자 별도 승인 없이 실행하지 않는다.
- 운영 주문, 사용자, 쿠폰 등 실제 데이터를 테스트 데이터로 사용하거나 변경하지 않는다.
- 운영 index, 데이터, Rules를 테스트용 파일이나 설정으로 덮어쓰지 않는다.
- 백필, 마이그레이션, 실제 계정 생성·연결, 데이터 구조 변경은 별도 분석과 명시적 승인 후에만 수행한다.
- 작업 중 운영 프로젝트 접근이 감지되면 즉시 중단하고 접근 종류와 영향을 보고한다.

## Emulator를 사용하는 테스트 작업의 안전규칙

이 섹션은 Emulator를 실제로 사용하는 테스트 작업에만 적용한다. 이 규칙은 저장소 전체가 Emulator 전용이라는 뜻이 아니다.

- Firebase 프로젝트 ID는 `demo-haetsalmaru`만 사용한다.
- Firestore Emulator는 `127.0.0.1:8080`에 연결한다.
- Authentication Emulator는 `127.0.0.1:9099`에 연결한다.
- Emulator UI는 `127.0.0.1:4000`을 사용한다.
- 테스트 앱은 `localhost` 또는 `127.0.0.1`에서만 실행한다.
- 테스트 데이터에는 실제 개인정보, 운영 데이터, 실제 운영 계정을 사용하지 않는다.
- 테스트 과정에서 운영 프로젝트 접근이 0건인지 확인한다.
- 실행 명령과 검증 절차는 현재 작업환경에 실제로 존재하는 설정과 스크립트를 확인한 뒤 사용한다.

## UX/UI 원칙

- 처음 사용하는 사람도 별도 설명 없이 주요 기능의 의미와 다음 행동을 이해할 수 있어야 한다.
- 버튼, 메뉴, 상태 표현은 명확하게 한다.
- 자주 쓰는 기능의 접근성을 높이고 불필요한 클릭, 입력, 화면 이동을 줄인다.
- 작업 결과와 현재 상태를 사용자가 이해할 수 있게 표시한다.
- 실수하기 쉬운 조작은 예방하거나 되돌릴 수 있는 방법을 우선 검토한다.
- 스마트폰과 태블릿의 실제 터치 환경을 고려한다.
- 기존 디자인과의 일관성을 유지한다.

## 호환성 및 성능 검증

수정 후 가능한 범위에서 다음을 확인한다.

- iOS Safari와 Android Chrome
- 스마트폰과 태블릿의 다양한 viewport
- 가로와 세로 방향
- 터치, 입력, 스크롤, 팝업 접근성
- 가상 키보드로 인한 화면 가림
- 화면 잘림과 의도하지 않은 가로 스크롤
- 기존 기능의 성능 회귀

지원 여부가 불확실한 최신 HTML, CSS, JavaScript 기능을 임의로 도입하지 않는다.

## 수정 후 검증

가능한 범위에서 다음을 확인한다.

- 문법 오류
- 목표 증상 해결 여부
- 관련 기존 기능의 회귀 여부
- 예상하지 않은 데이터 읽기와 쓰기
- 콘솔 오류
- 화면 크기별 UI 문제
- 운영 데이터와 운영 프로젝트 접근 여부

- 검증하지 못한 항목은 절대로 PASS로 처리하지 않고 `⚠️ 미검증` 또는 `UNVERIFIED`로 표시한다.
- 실패한 검증은 실패 단계, 오류, 관련 상태와 함께 `FAIL`로 보고한다.
- 검증이 완료되지 않았다면 단순히 완료됐다고 표현하지 않는다.

## ChatGPT, Codex, GitHub 협업

- 사용자가 ChatGPT에서 문제 분석과 수정 범위를 정한 뒤 승인하면 Codex가 실제 수정을 수행하는 흐름을 기본으로 한다.
- GitHub의 코드, 커밋, PR과 기타 작업 기록은 ChatGPT가 이후 결과를 검토할 수 있는 공용 기록으로 사용한다.
- 개별 작업지시가 있으면 그 지시에서 명시적으로 승인된 범위가 우선한다.
- 작업 완료 보고에는 가능한 범위에서 다음을 남긴다.
  - 수정 파일
  - 실제 변경 내용
  - 수행한 검증
  - `PASS`, `FAIL`, `UNVERIFIED`
  - 발견했지만 승인 범위 밖이라 수정하지 않은 문제

## Git, 배포 및 공개

- commit, push, PR, 배포는 해당 작업지시에 포함된 경우에만 수행한다.
- Git 작업 시 승인된 파일만 스테이징하고, 커밋 전 변경 파일 목록을 확인한다.
- GitHub Pages, Firebase Hosting 등 실제 배포는 사용자 명시적 승인 없이 수행하지 않는다.
- 특허 가능성을 검토할 핵심 기능은 광범위하게 외부 공개하기 전에 지식재산권 검토가 필요하다는 장기 원칙을 유지한다.

## 지시 충돌 우선순위

충돌 시 다음 순서로 따른다.

1. 사용자의 현재 최신 명시적 지시
2. 현재 작업에서 승인된 범위
3. `AGENTS.md`의 장기 원칙
4. 기존 구현 관례

범위가 불분명하면 임의로 확장하지 않고 수정하지 않은 상태로 보고한다.
